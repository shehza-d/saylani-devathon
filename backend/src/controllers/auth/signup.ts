import type { Request, Response, NextFunction } from "express";

export const signupHandler = async (req: Request, res: Response) => {
  console.log("🚀 ~ file: signup.ts:4 ~ signup ~ req:", req);
};
